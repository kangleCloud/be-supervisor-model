# be-supervisor-model 业务流程说明

## 1. 背景与目标

`be-supervisor-model` 是一个面向运维后台的管理服务，不承担业务系统本身的业务逻辑。

当前系统聚焦两类能力：

- 登录鉴权：提供登录、当前登录态查询、退出登录，并把登录令牌与登录审计写入 MySQL
- Supervisor 管理：纳管目标主机上的 Supervisor 服务配置，并提供读取和新增接口

系统目标是为后续前端管理后台或内部运维工具提供统一的 HTTP 接口，用于：

- 限定白名单主机范围
- 安全生成 `/etc/supervisord.d` 下的 Supervisor 配置
- 将配置主数据落库，避免列表长期依赖远端全量扫目录
- 执行受控的 `supervisorctl reread/update/status`

## 2. 角色与职责

| 角色/组件 | 职责 |
| --- | --- |
| 运维管理员 | 登录后台后查询和新增 Supervisor 服务 |
| `be-supervisor-model` | 统一处理登录鉴权、主机白名单校验、配置模板渲染、远端写文件、主数据落库和状态聚合 |
| MySQL 8 | 存储用户、JWT 登录令牌、登录审计和 Supervisor 服务主数据 |
| `local` 执行器 | 当管理服务与目标应用同机部署时，直接在本机执行文件操作和 `supervisorctl` |
| `ansible` 执行器 | 当目标主机为远程机器时，通过 `ansible` ad-hoc 命令执行受控文件与命令操作 |
| 被管理主机 | 提供 `/etc/supervisord.d` 配置目录和 `supervisorctl` 命令环境 |

## 3. 认证与令牌流程

### 3.1 认证接口

认证接口统一挂载在：

```text
/admin/api/auth/*
```

当前提供：

- `POST /admin/api/auth/login`
- `GET /admin/api/auth/profile`
- `POST /admin/api/auth/logout`

### 3.2 登录链路

1. 运维管理员调用 `/admin/api/auth/login`，提交用户名和密码。
2. 服务从 `sys_user` 表读取账号信息，校验账号存在、未删除、状态为启用。
3. 服务校验密码哈希。
4. 校验通过后签发 JWT 访问令牌。
5. 服务把登录令牌写入 `sys_login_token`，只保存 `token_jti` 和 `token_digest`，不保存原始 token 明文。
6. 服务更新 `sys_user` 的最近登录时间和登录地址。
7. 服务把本次成功或失败登录写入 `sys_login_log`。
8. 调用方后续访问受保护接口时，统一通过 `Authorization: Bearer <jwt>` 传递访问令牌。

### 3.3 令牌校验与退出

1. 受保护接口统一校验 Bearer JWT。
2. 服务先校验 JWT 签名和过期时间，再根据 `sub + jti` 到 `sys_login_token` 查询活动令牌。
3. 令牌存在且 `token_digest` 匹配时，才允许继续访问。
4. `/admin/api/auth/logout` 会把当前令牌写入 `revoked_time`，使同一 JWT 后续请求立即失效。

### 3.4 认证相关数据表

当前登录鉴权依赖以下表：

- `sys_user`
- `sys_login_token`
- `sys_login_log`
- `sys_schema_migration`

数据库启动规则：

- 服务启动时自动创建业务库（若不存在）
- 自动执行 `app/database/migrations/*.sql`
- 自动初始化一条默认超级管理员：`admin / Admin@123456`

## 4. Supervisor 管理流程

### 4.1 Supervisor 接口

Supervisor 管理接口统一挂载在：

```text
/admin/api/supervisor/*
```

当前提供：

- `GET /admin/api/supervisor/hosts`
- `GET /admin/api/supervisor/services`
- `GET /admin/api/supervisor/services/{programName}`
- `POST /admin/api/supervisor/services`

### 4.2 主数据来源

Supervisor 管理采用“数据库为主、远端文件为执行结果”的模型：

- `sys_supervisor_service`：保存纳管服务的结构化配置主数据
- `/etc/supervisord.d/*.ini`：保存已写入目标主机的实际 Supervisor 配置
- `supervisorctl status`：返回运行状态

列表和详情接口固定先查 `sys_supervisor_service`，再补充远端文件状态与运行状态，不再以“扫远端目录结果”作为主视图。

### 4.3 主机白名单与执行模型

目标主机必须来自配置中的 `hosts` 白名单，不允许调用方传入任意执行目标。

每台主机都绑定一种执行方式：

- `local`：直接在本机操作配置与执行命令
- `ansible`：通过 `ansible` 对远程主机执行受控文件与命令操作

执行器仅暴露受控能力：

- 列出配置文件
- 读取配置文本
- 原子写入配置文件
- 删除文件
- 执行固定格式的 `supervisorctl` 命令

### 4.4 配置文件目录与命名规则

Supervisor 配置目录固定为：

```text
/etc/supervisord.d
```

受控扫描范围包括：

- `*.ini`
- `*.ini.bak`
- `*.ini.bak.*`

默认命名规则：

- `programName = {jobName}_{moduleName}`
- `configName = {programName}.ini`

服务端负责：

- 校验 `jobName / moduleName / programName / configName` 安全正则
- 拒绝 `../`、绝对路径注入和备份文件名伪装
- 把配置文件路径限制在 `/etc/supervisord.d`

### 4.5 模板渲染规则

当前新增接口允许调用方输入的主要字段为：

- `host`
- `jobName`
- `moduleName`
- `javaPath`
- `active`
- `port`
- `jarName`
- `configName`
- `xms`
- `xmx`
- `user`

以下 Supervisor 模板参数固定为系统内建策略，不接受前端自定义输入：

- `autostart=true`
- `startsecs=10`
- `autorestart=true`
- `startretries=3`
- `priority=999`
- `redirect_stderr=true`
- `stdout_logfile_maxbytes=1GB`
- `stdout_logfile_backups=1`
- `stopasgroup=false`
- `killasgroup=false`

### 4.6 新增服务流程

1. 校验 Bearer JWT 与令牌状态。
2. 校验目标主机必须在白名单中。
3. 根据请求变量生成 `programName` 和 `configName`。
4. 先校验数据库中同主机下的 `programName / configName / port` 不能重复。
5. 检查远端目标配置文件是否已存在。
6. 扫描 `/etc/supervisord.d` 下的 `.ini/.ini.bak/.ini.bak.*`，检测端口冲突，防止库外人工文件撞端口。
7. 按固定模板渲染 Supervisor 配置。
8. 原子写入目标配置文件。
9. 执行 `supervisorctl reread`。
10. 执行 `supervisorctl update`。
11. 远端成功后，把结构化配置写入 `sys_supervisor_service`。
12. 如果写库失败，立即删除刚写入的 `.ini`，再执行一次 `reread/update` 回滚现场。

### 4.7 服务读取流程

服务列表和详情接口会合并三部分信息：

- 数据库视角：`sys_supervisor_service` 中的结构化主数据
- 配置文件视角：按数据库记录实时渲染的 `expectedContent`，以及远端实际文件是否匹配
- 运行状态视角：`supervisorctl status` 返回结果

远端文件状态固定分为：

- `MATCH`：远端文件存在且与数据库渲染结果完全一致
- `MISSING`：数据库有记录，但远端 `.ini` 不存在
- `MISMATCH`：远端 `.ini` 存在，但内容与数据库渲染结果不一致

### 4.8 导入现网配置

为了切换到“库为主”的读取模型，现网已有配置需要先执行一次导入脚本。

导入规则：

- 只导入 `*.ini`
- 不导入 `.bak` 和归档备份
- 只有能完整反解 `jobName/moduleName/javaPath/active/port/jarName/xms/xmx/user` 的配置才允许入库

## 5. 配置与运行模型

### 5.1 配置来源

当前配置采用：

- `config.yaml`
- 环境变量覆盖

环境变量优先级高于 YAML。

### 5.2 关键配置项

应用与认证相关：

- `APP_HOST`
- `APP_PORT`
- `APP_LOG_LEVEL`
- `DATABASE_HOST`
- `DATABASE_PORT`
- `DATABASE_NAME`
- `DATABASE_USER`
- `DATABASE_PASSWORD`
- `JWT_SECRET`
- `ACCESS_TOKEN_EXPIRE_MINUTES`

Supervisor 与执行器相关：

- `SUPERVISOR_CONF_DIR`
- `COMMAND_TIMEOUT_SECONDS`
- `EXECUTOR_TYPE`
- `ANSIBLE_INVENTORY_PATH`
- `ANSIBLE_REMOTE_USER`

## 6. 安全边界

当前系统的核心安全边界如下：

- 目标主机必须来自配置白名单
- 禁止任意命令执行
- 禁止路径穿越
- 所有配置文件操作必须限制在 `/etc/supervisord.d`
- `programName/jobName/moduleName/configName` 必须满足安全正则
- 所有 shell 参数必须经过受控构造，不能直接拼接用户输入
- 默认不记录敏感凭证
